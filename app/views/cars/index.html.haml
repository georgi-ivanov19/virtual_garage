%h1= t('title')
%body
- @cars.each do |car|
  .col-sm-12.col-md-6.col-lg-4
    .container-fluid.cars
      = link_to (image_tag car.image.url, class: 'img-responsive'), car
      .caption
        %p 
          = car.make + ', ' + car.model + ', ' + car.engine

